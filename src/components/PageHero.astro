---
const {
  eyebrow = '',
  title = '',
  description = '',
  image = '/logoama.png'
} = Astro.props;
---

<section class="relative overflow-hidden bg-gradient-to-br from-primary-50 via-white to-white py-28">
  <div class="absolute inset-0">
    <div class="absolute -top-32 -left-32 h-72 w-72 rounded-full bg-primary-100/40 blur-3xl" />
    <div class="absolute -bottom-32 -right-24 h-72 w-72 rounded-full bg-primary-200/30 blur-3xl" />
  </div>
  <div class="relative mx-auto flex max-w-6xl flex-col gap-10 px-6 text-center lg:flex-row lg:items-center lg:gap-16 lg:text-left">
    <div class="space-y-6 animate-fade-up">
      {eyebrow && (
        <span class="inline-flex items-center rounded-full border border-primary-200/70 bg-white px-4 py-1 text-xs font-semibold uppercase tracking-[0.4em] text-primary-500">
          {eyebrow}
        </span>
      )}
      <h1 class="font-display text-3xl font-semibold text-dark sm:text-4xl lg:text-[3rem]">
        {title}
      </h1>
      {description && <p class="text-base text-dark/70 sm:text-lg">{description}</p>}
    </div>
    <div class="mx-auto flex max-w-xs flex-col items-center rounded-[36px] border border-primary-100 bg-white/85 p-6 shadow-glow backdrop-blur animate-fade-up animate-delay-150 lg:mx-0">
      <img src={image} alt="Ilustración representativa" class="h-28 w-auto" />
      <p class="mt-4 text-sm text-dark/60">
        Espacios tejidos con sensibilidad, ciencia y espiritualidad.
      </p>
    </div>
  </div>
</section>
